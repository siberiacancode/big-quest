const COOKIE_PREFIX = 'big-quest';

export const COOKIES = {
  SESSION_ID: 'connect.sid',
  THEME: `${COOKIE_PREFIX}-theme`,
  OPEN_SIDEBAR: `${COOKIE_PREFIX}-sidebar`,
  USER_SESSION: `${COOKIE_PREFIX}-userSession`
};
