export interface ActivitiesTableRow {
  id: string;
  organization: string;
  activity: string;
  location: string;
  status: string;
  category: string;
  type: string;
}

// export const convertActivitiesToTableRows = (
